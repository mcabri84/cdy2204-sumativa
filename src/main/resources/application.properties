spring.application.name=cdy2204-sumativa

server.port=8080

spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:cdy2204db}
spring.datasource.username=${DB_USER:cdy2204user}
spring.datasource.password=${DB_PASSWORD:Cdy2204Pass2026}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

aws.region=${AWS_REGION:us-east-1}
aws.s3.bucket=${AWS_S3_BUCKET:cdy2204s3}

app.efs.path=${EFS_PATH:/mnt/efs/guias}
app.file.prefix=guias

spring.rabbitmq.host=${RABBITMQ_HOST:localhost}
spring.rabbitmq.port=${RABBITMQ_PORT:5672}
spring.rabbitmq.username=${RABBITMQ_USER:cdy2204}
spring.rabbitmq.password=${RABBITMQ_PASSWORD}
spring.rabbitmq.listener.simple.default-requeue-rejected=false


oracle.datasource.url=${ORACLE_JDBC_URL}
oracle.datasource.username=${ORACLE_DB_USER}
oracle.datasource.password=${ORACLE_DB_PASSWORD}

spring.rabbitmq.listener.simple.default-requeue-rejected=false

azure.b2c.jwk-set-uri=${AZURE_B2C_JWK_SET_URI:https://gestionguiasmcabri2026.b2clogin.com/gestionguiasmcabri2026.onmicrosoft.com/discovery/v2.0/keys?p=B2C_1_guias_signupsignin}
azure.b2c.audience=${AZURE_B2C_AUDIENCE:89ac1d95-26f7-4e26-afba-92b4b364f87f}
